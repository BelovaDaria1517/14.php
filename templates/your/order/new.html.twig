{% extends 'base.html.twig' %}

{% block title %}Создать заказ{% endblock %}

{% block body %}
    <h1>Создать заказ</h1>

    {{ form_start(form) }}
        <div class="form-group">
            {{ form_label(form.client) }}
            {{ form_widget(form.client) }}
        </div>

        <div class="form-group">
            {{ form_label(form.dishes) }}
            {{ form_widget(form.dishes) }}
        </div>

        <button class="btn btn-primary">Создать заказ</button>
        <a href="{{ path('app_order_index') }}" class="btn btn-secondary">Отмена</a>
    {{ form_end(form) }}
{% endblock %}